Given(/^there is one list named "([^"]+)" with (\d+) closed random tasks?$/) do |listName, tasksNumber|
  pending
end

When(/^"([^"]+)" will be reopened$/) do |listName|
  pending
end

Then(/^all tasks on "([^"]+)" should be opened$/) do |listName|
  pending
end

Then(/^"([^"]+)" should be opened$/) do |listName|
  pending
end